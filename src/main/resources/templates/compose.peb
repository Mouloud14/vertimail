<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Nouveau Message</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body style="background-color: var(--bg-color); padding: 40px;">

    <div class="form-card">
        <h2 style="margin-top:0;">Nouveau Message ✍️</h2>

        <form method="post">
            <input type="hidden" name="sender" value="{{ username }}">

            <div class="form-group">
                <label>Destinataire :</label>
                <input type="text" name="recipient" placeholder="..." value="{{ recipient }}">
            </div>

            <div class="form-group">
                <label>Sujet :</label>
                <input type="text" name="subject" placeholder="Objet de votre message" value="{{ subject }}">
            </div>

            <div class="form-group">
                <label>Message :</label>
                <textarea name="content" style="height: 200px;">{{ content }}</textarea>
            </div>

            <div style="display: flex; justify-content: space-between; align-items: center;">
                <div>
                    <button type="submit" formaction="/send" class="btn btn-primary">Envoyer 🚀</button>
                    <button type="submit" formaction="/draft" class="btn btn-warning">Sauvegarder Brouillon 💾</button>
                </div>
                <a href="/box?username={{ username }}&folder=inbox" class="btn btn-cancel">Annuler</a>
            </div>
        </form>
    </div>

</body>
</html>
