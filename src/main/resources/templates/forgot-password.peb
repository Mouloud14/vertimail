<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mot de passe oublié - Vertimail</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="auth-page">

<div class="auth-box">
    <h2 style="color: var(--primary-color);">Vertimail</h2>
    <h3>Réinitialisation du mot de passe</h3>

    {% if error %}
        <p style="color: var(--danger-color); margin-top: 15px; font-weight: 500;">{{ error }}</p>
    {% endif %}

    <form action="/forgot-password" method="post" style="margin-top: 30px;">
        <div class="form-group">
            <label for="username">Entrez votre nom d'utilisateur</label>
            <input type="text" id="username" name="username" class="form-control" required>
        </div>
        <button type="submit" class="btn">Continuer</button>
    </form>

    <a href="/" class="btn btn-secondary">Retour à la connexion</a>
</div>

</body>
</html>
